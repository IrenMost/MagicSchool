import { createContext } from 'react';


export const DataContext = createContext(null); // user object (currentCourse, firstName, lastname, identityUser, IdentityUserId, last name, level, teacherId)
                                                // vagy épp


